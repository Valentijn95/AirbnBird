<%= simple_form_for bird do |f| %>
   <%= f.input :title %>
   <%= f.input :dedscription, as: :text, :input_html => { :cols => 5, :rows => 10 } %>
   <%= f.input :dedscription, as: :text, :input_html => { :cols => 5, :rows => 10 } %>
   <%= f.button :submit, class: 'btn btn-primary'%>
<% end %>
